import { BN_ASSERT_DECIMALS_OP } from '../enums';
export declare const assertDecimals: (currentDecimals: number, targetDecimals: number, op: BN_ASSERT_DECIMALS_OP) => void;
export declare const assertDecimalsInteger: (decimals: number) => void;
