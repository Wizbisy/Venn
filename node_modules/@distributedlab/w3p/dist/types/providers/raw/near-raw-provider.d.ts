import { type Wallet, type WalletSelector } from '@near-wallet-selector/core';
import type { providers } from 'near-api-js';
import { type NearTxRequestBody } from '../../types';
export declare class NearRawProvider {
    selector: WalletSelector | null;
    wallet: Wallet | null;
    createAccessKeyFor: string;
    accountId: string;
    isNear: boolean;
    constructor({ createAccessKeyFor }: {
        createAccessKeyFor?: string;
    });
    init(): Promise<boolean>;
    signIn(): Promise<void>;
    signOut(): Promise<void>;
    signAndSendTxs(txBody: NearTxRequestBody): Promise<providers.FinalExecutionOutcome[]>;
}
